<jboss-web>

	<!-- Uncomment the security-domain to enable security. You will need to 
		edit the htmladaptor login configuration to setup the login modules used 
		to authentication users. <security-domain>java:/jaas/jmx-console</security-domain> 
		<resource-ref> <res-ref-name>jdbc/test</res-ref-name> <jndi-name>java:/jdbc/test</jndi-name> 
		</resource-ref> <resource-ref> <res-ref-name>jdbc/DefaultDS</res-ref-name> 
		<res-type>javax.sql.DataSource</res-type> <jndi-name>java:/DefaultDS</jndi-name> 
		</resource-ref> -->


	<context-root>/</context-root>
	<virtual-host>www.preliminary.com</virtual-host>
	<server-instance>default-server</server-instance> <!-- does mapping to server -->



</jboss-web>